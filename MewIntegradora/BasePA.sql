create database BasePA;
use BasePA;
create table BasePA(nombre varchar(20),
					apellido varchar(30),
                    telefono varchar(13),
                    correo varchar(30),
                    contrasenia varchar(30),
                    edad varchar(2),
                    sexo varchar(15)
                    );
select * from BasePA;
